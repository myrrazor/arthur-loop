# Executor: claude-code

Same contract as the codex executor — one approved packet, tests plus evidence, control block last. Run headless (`claude -p`) or interactive; keep one session (or git worktree) per project so implementations don't cross streams.
