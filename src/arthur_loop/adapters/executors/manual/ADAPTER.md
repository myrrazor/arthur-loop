# Executor: manual

You implement the approved packet yourself, then write the implementation handoff — with its control block — to a file and `arthur capture` it. Useful when the loop plans and reviews but a human writes the code.
