interface:
  display_name: "Arthur Loop"
  short_description: "Run advisor/executor project loops with durable file state."
  default_prompt: "Use Arthur Loop to coordinate an advisor review loop, executor coding-session handoffs, durable queue state, human-decision escalation, and quota-aware task tracking."
